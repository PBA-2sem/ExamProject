const router = require('express').Router();
const auth = require('../auth');

// Add to products

// remove from products

// update product

// get products

// get by category

module.exports = router;